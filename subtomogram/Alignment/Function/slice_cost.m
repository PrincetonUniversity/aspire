function [ func ] = slice_cost( coeff, alpha, gamma)
% compute the euclidean distance between two image originaly on the xy
% plane and rotated to ZYZ (alpha, beta, gamma) angles between them.

Lmax = size(coeff_pos_k,1);




end

